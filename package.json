{
  "name": "comet",
  "version": "0.1.0",
  "description": "COMET connects Codex to Obsidian-compatible Markdown memory and knowledge graphs.",
  "repository": "https://github.com/Avery320/codex-vault.git",
  "homepage": "https://github.com/Avery320/codex-vault#readme",
  "bugs": {
    "url": "https://github.com/Avery320/codex-vault/issues"
  },
  "author": "Avery Tsai",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "reset": "yarn && yarn clean && yarn build",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "test": "lerna run test",
    "test:unit": "lerna run test:unit",
    "test:e2e": "lerna run test:e2e",
    "lint": "lerna run lint",
    "format": "prettier --write \"packages/comet-core/**/*.ts\" \"packages/comet-graph/**/*.ts\" \"packages/comet-mcp/**/*.ts\" \"packages/comet-cli/**/*.ts\"",
    "mcp": "./scripts/launch_codex_vault_mcp"
  },
  "devDependencies": {
    "lerna": "^8.2.2"
  },
  "engines": {
    "node": ">=22"
  },
  "prettier": {
    "arrowParens": "avoid",
    "printWidth": 80,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5"
  }
}
