name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint active packages
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: yarn
      - run: yarn install --frozen-lockfile
      - run: yarn workspace @foam/core lint
      - run: yarn workspace @foam/graph-view lint
      - run: yarn workspace @foam/mcp lint
      - run: yarn workspace @foam/cli lint

  test:
    name: Build and test Codex Vault
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: yarn
      - run: yarn install --frozen-lockfile
      - run: yarn build
      - run: yarn workspace @foam/core test:unit
      - run: yarn workspace @foam/graph-view test:unit
      - run: yarn workspace @foam/mcp test
      - run: yarn workspace @foam/cli test
